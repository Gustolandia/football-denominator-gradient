% The anonymised supplement: referees receive the supplement alongside the
% manuscript, so it needs the same treatment. Review is single-anonymous, so
% this claims no anonymity; it keeps author identity on the separate title
% page. Defining \journalblindcopy blanks the author line.
\def\journalblindcopy{1}
\def\journalreviewcopy{1}
\input{supplement.tex}
