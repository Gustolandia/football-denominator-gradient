% The anonymised, line-numbered review copy. Science and Medicine in Football
% reviews single-anonymously, so referees may see who wrote this and no
% anonymity is being claimed here; the copy exists because author identity
% still belongs on the separate title page rather than scattered through the
% manuscript, and because a preprint already defeats anonymity in any case.
% Defining \journalblindcopy blanks the author block and switches the back
% matter to its identity-free variants; \journalreviewcopy adds line numbers so
% referees can point at lines.
\def\journalblindcopy{1}
\def\journalreviewcopy{1}
\input{manuscript.tex}
